#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {

    int arr[] = {3, 5, 0, 0, 4};
    int n=sizeof(arr)/sizeof(arr[0]);
    int d;
    for (int i = 0; i < n; i++)
    {
       arr[i]=arr[n];
       d=
    }
    for (int i = 0; i < n; i++)
    {
       cout<< arr[i];
    }
    

  return 0;
}